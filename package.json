{
  "name": "hono-mongodb",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p .",
    "build:esbuild": "rm -rf dist && node esbuild.config.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.5",
    "dotenv": "^16.4.5",
    "hono": "^4.6.9",
    "mongoose": "^8.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "esbuild": "^0.24.0",
    "tsx": "^4.7.1",
    "typescript": "^5.6.3"
  }
}
